name: konversi_suhu
description: Aplikasi Konversi Suhu Sederhana
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5

  ## shared preferences
  shared_preferences: ^2.3.0

  ## firebase
  firebase_core: ^3.15.2
  firebase_auth: ^5.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true